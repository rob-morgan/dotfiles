- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.config:
    ~/.p10k.zsh:
    ~/.zshenv:
    ~/.zprofile:
    ~/.zshrc:
    ~/.zlogin:
    ~/.zlogout:
    ~/.zpreztorc:

- shell:
    - [git submodule update --init --recursive, Installing submodules]
